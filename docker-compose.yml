services:
  language:
    build: ./apps/language
    ports:
      - "8000:8000"

  scheduling:
    build: ./apps/scheduling
    ports:
      - "3000:3000"

  web:
    build: ./apps/web
    ports:
      - "5173:5173"

